import React from "react";
import { ThemeProvider } from "../../../styles/theme";
import { Hero } from "../../../styles/common";
//import './.css';

const CloudBasedServices = props => {
  return (
    <ThemeProvider>
      <Hero>CloudBasedServices</Hero>
    </ThemeProvider>
  );
};
export default CloudBasedServices;
